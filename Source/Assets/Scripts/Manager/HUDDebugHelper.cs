using UnityEngine;

public class HUDDebugHelper : MonoBehaviour
{
    // TODO : Debug 관련 함수 추가 분리?
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
